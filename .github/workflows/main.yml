name: Trigger Netlify Build
on:
  schedule:
    - cron: "0 0,12 * * *" # at 12 and 24h
jobs:
  build:
    name: Request Netlify Webhook
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl -X POST -d {} scheduled_hook
